language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-teslar' ]; then cd .. && eval "mv $currentfolder generator-teslar" && cd generator-teslar; fi

